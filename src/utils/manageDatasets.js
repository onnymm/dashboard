import { useCategoryDataset } from "../custom hooks/displayDatasets"

export const setDatasetSelection = (dataContainer) => {
    if ( dataContainer.series.datasets.length > 1 ) {
        return (useCategoryDataset)
    }
}